<%= form_for(@psycho) do |f| %>
  <% if @psycho.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@psycho.errors.count, "error") %> prohibited this psycho from being saved:</h2>

      <ul>
      <% @psycho.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
